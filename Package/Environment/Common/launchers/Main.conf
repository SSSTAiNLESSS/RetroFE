executable = %ITEM_FILEPATH%
arguments =

# reboot = yes
#   Restart RetroFE once this launcher finishes, so a settings change made by
#   the launched script is picked up. Used by on-screen settings menus, not by
#   game launchers. Set restoreStateOnReboot = yes in settings.conf to come
#   back to the menu you were in rather than the first collection.
#   See docs/RetroFE/Settings-Menu-Reboot-Restore.md
